<?xml version="1.0" encoding="utf-8"?>
<modification>
    <code>cdek-official</code>
    <name>CDEK Shipping Official</name>
    <version>dev</version>
    <author>CDEK IT</author>
    <link>https://www.cdek.ru/ru/integration</link>
    <file path="catalog/view/theme/default/template/checkout/shipping_method.twig">
        <operation>
            <search>
                <![CDATA[{% for shipping_method in shipping_methods %}]]>
            </search>
            <add position="after">
                <![CDATA[
                    {% if shipping_method.extra %}
                        {{ shipping_method.extra }}
                    {% endif %}
                ]]>
            </add>
        </operation>
        <operation>
            <search>
                <![CDATA[{{ quote.title }} - {{ quote.text }}]]>
            </search>
            <add position="replace">
                <![CDATA[
                    {{ quote.title }} - {{ quote.text }}
                    {% if quote.extra %}
                        {{ quote.extra }}
                    {% endif %}
                ]]>
            </add>
        </operation>
    </file>
</modification>
